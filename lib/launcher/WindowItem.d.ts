import { PropsWithChildren } from 'react';
interface WindowItemProps {
    windowId: string;
}
export declare function WindowItemView({ windowId, children }: PropsWithChildren<WindowItemProps>): JSX.Element;
export {};
